[package]
name = "noviscia-credit-line-sdk"
version = "0.1.0"
edition = "2021"
description = "Low-latency instruction builder for the Noviscia Credit Line (atomic single-block credit)."
license = "Apache-2.0"

[dependencies]
solana-program = "4"
spl-token = "7"
noviscia-types = { version = "0.2.2", path = "../noviscia-types" }

[lib]
name = "noviscia_credit_line_sdk"
