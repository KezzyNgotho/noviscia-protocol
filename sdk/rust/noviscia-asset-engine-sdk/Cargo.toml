[package]
name = "noviscia-asset-engine-sdk"
version = "0.2.0"
edition = "2021"
description = "Multi-asset JIT allocation builders & PDA derivation for the Noviscia asset engine."
license = "Apache-2.0"

[dependencies]
solana-sdk = "2"
solana-program = "2"
anchor-lang = "0.31.1"
spl-token = "7"
noviscia-types = { version = "0.2.0", path = "../noviscia-types" }