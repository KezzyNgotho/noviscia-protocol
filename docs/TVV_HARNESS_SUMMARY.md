## Objective
- Build the on-chain suite for the Noviscia TVV engine in the fresh workspace `noviscia-tvv` (gate `noviscia_tvv_gate`, pool `noviscia_omni_pool`) and get a running localnet integration test proving: atomic invariant lock, zero-leakage settlement, 24h 2/20 + 15% DIF clearing, NAV appreciation with highwater.
- Then ship the off-chain control plane: position matrix mirror, sentinel (sweep/settle_day/collect/ttl-watch), operator CLI, and a LIVE Yellowstone gRPC ingest seam (geyser plugin + native N-API client).
- Current focus (Phase 1, DONE): align implementation with the "4-Cluster Engine" spec — bring the CCP clearing core onto the gate: `RiskState` PDA (JLF 50% circuit breaker, emergency halt, aggregate exposure + per-asset concentration caps, committed-exposure ledger), a structural DIF 20% treasury (`collect_dif` to `[b"tvv-dif"]`), and mirror the whole thing in the off-chain matrix/sentinel + live specs.
- Cluster 1 searcher margin & netting engine (Phase 2, DONE): `MarginState` PDA per searcher (collateral escrow, gross/hedged exposure, maintenance bps, liveness flag), `post_margin`/`withdraw_margin`/`set_hedge`, live margin check in `pull_cap`, gross decrement + flag recompute on settle/force_close, and the cross-cluster gateway handshake (`RiskState.gateway` pinned to the gate program id on init, enforced in pull/settle/force_close bodies). Verified on-chain 20/20 + off-chain 12/12.
- Cluster 3 → 2 supply pump (Phase 3, DONE): the omni vault becomes the gate's lender — `deploy_supply` pushes LP capital into the gate reserve (with a `gate_loan` ledger), `recall_supply` CPI's the gate's new `repay_supply` to repay principal back to the vault, and the gate's `register_lender` pins the single allowed lender pool (`GateConfig.lender_pool`). Borrowing the deployed supply banks real yield through the CCP window. Verified on-chain 28/28 + off-chain 13/13.

## Important Details
- Program ids: gate `A1cYVV1PjhHgJfiKZzPsjo6Bf7E8NNkMdiv4xNkTaU9g`; omni `2t5zfcgpdWW5MH4PrQjRDyqVB5MhbwwKFFQJN3VZogAr`. Deployed from `.keys/*.json` (wallet `~/.config/solana/new-id.json`).
- Solana/Agave CLI `2.1.0`; geyser plugin = `libyellowstone_grpc.so` from the `release22` (Ubuntu 22 / OpenSSL3, `libssl.so.3`) tarball of `rpcpool/yellowstone-grpc` tag `v5.0.1+solana.2.1.16`, now at `~/.local/lib/`. The plain `release` asset links `libssl.so.1.1` and WILL NOT load on Debian. Plugin config `~/.local/etc/geyser-config.json` (required field: `tokio.affinity`; `grpc.address` is the bind; no top-level `filter` — filtering is client-side). Validator started with `solana-test-validator --geyser-plugin-config <cfg> --bpf-program <id> <so> ...`, geyser gRPC listens on `127.0.0.1:10000`.
- `@triton-one/yellowstone-grpc` **v7.0.1**: native N-API client (bundled `.node`), NO grpc-js. API: `const client = new Client(endpoint, xToken, { grpcMaxDecodingMessageSize }); await client.connect(); const stream = await client.subscribe(); await write(request); stream.on('data', update)`. Endpoint needs `http://` scheme. `SubscribeRequest` requires ALL maps (`accounts`,`slots`,`transactions`,`transactionsStatus`,`entry`,`blocks`,`blocksMeta`) + `accountsDataSlice` + `commitment`. Slot statuses: on localnet a slot goes processed→finalized with NO `confirmed` event, so slots filters MUST use `filterByCommitment: false` or slot updates never arrive. `SubscribeRequestFilterAccounts` filters are per-public-key OR-list.
- `assets` field on gate mirror is a `PublicKey[]` (Anchor decodes). `PositionMatrix.connect` auto-derives usdc mint from `gate.assets[0]`; matrix exposes `gatePda`/`vaultPda`/`usdcMint` publicly for ingest watch-key wiring.
- Integration suite uses a **persisted searcher keypair** at `.searcher-key.json` (created by the suite if absent; gitignored). Off-chain derives run/searcher PDAs from its pubkey — both suites must share it.
- NVSC is governance-only; vault LP = nTokens (mint PDA `[b"nshares", usdc_mint]`, 6 dec, authority = vault PDA `[b"omni-vault", usdc_mint]`). Gate seeds: `b"tvv-gate"`, `b"tvv-searcher"`, `b"tvv-run"`; Phase 1 adds `b"tvv-risk"` (RiskState PDA, seed `[tvv-risk, gate]`, SPACE 141) and `b"tvv-dif"` (DIF treasury PDA, seed `[tvv-dif, gate]`, used only as the dif_vault ATA owner — NO data account).
- Phase 1 gate params/econ: `GateConfig += dif_ledger: u128` APPENDED after `swept_yield` (+16 SPACE) → gate mirrored `realized_yield` stays at the omni raw-offset `[8+68..8+84]`. `initialize` gains `(jlf_capital, max_exposure, max_asset_share_bps)` args and inits the RiskState account. Constants `DIF_BPS=2000` (20% of gross realized → `dif_ledger`, 80% → `realized_yield`) and `JLF_HALT_BPS=5000` (breaker trips when `jlf_losses*10000 >= jlf_capital*5000`, i.e. ≥50% drawdown; boundary inclusive: 400M losses on 800M capital trips). `force_close` is authority-gated, books the FULL principal into `jlf_losses` (conservative write-off; clawback of any stranded searcher-vault balance would count as yield on the NEXT settle), and `pull_cap` blocks when `emergency_halt || halted`, when `committed_total+amount > max_exposure`, or when `(committed_by_asset[i]+amount)/basis > max_asset_share_bps` with `basis = reserve + committed_total`.
- `set_risk_config` (authority): emergency_halt / max_exposure / max_asset_share_bps options + `jlf_top_up` (only ever INCREASES jlf_capital); recomputes `halted` every call. `collect_dif(amount)` (authority): reserve → dif_vault (ATA of the `[tvv-dif, gate]` treasury), ledger-capped, spent against `dif_ledger`.
- Risk-check ordering in pull_cap matters for revert matching: paused → asset whitelist → emergency_halt → halted → exposure → concentration → THEN searcher checks. So the smoke suite's revert regexes (`/emergency/`, `/exposure/`, `/share/`, `/breaker/`) hit the intended error.
- Margin engine (gate, seeds `b"tvv-margin"` → `[tvv-margin, searcher]`, SPACE 100): `MarginState { searcher, gate, collateral, gross_exposure, hedged_exposure, maintenance_bps, flag, bump }`, methods `net_exposure()` (`gross.saturating_sub(hedged)`), `margin_breached()` (`net*maint > collateral*10000`, u128, `>` — equality is EXACTLY covered), `projected_margin_breached(amount)` (for pull), `covers_margin(after)` (`>=` — the withdraw floor). `init_searcher(cap_bps, maintenance_bps, enabled)` inits the margin PDA. `withdraw_margin` order: `!flag` (/margin/) → `amount<=collateral` (/collateral/) → `covers_margin(remaining)` (/margin/). `set_hedge` clamps `hedged <= gross` (/hedge/) and is NOT flag-gated (it is how the flag can latch on an uncovered exposure). `RiskState.gateway: Pubkey` APPENDED (SPACE 141→173), set to `*ctx.program_id` at initialize, enforced as `risk.gateway == *ctx.program_id` (WrongGateway) inside pull/settle/force_close bodies.
- Three design constraints discovered while chasing a phantom +98M margin flow (all now reflected in the code/tests): (1) `splToken.createAccount` WITHOUT a keypair returns the **ATA**, so the "run vault" silently collapsed onto the searcher margin wallet — the run vault MUST be a raw account created with an explicit `Keypair`; (2) collateral lives in a margin **escrow** (ATA owned by the off-curve margin PDA), and the searcher's normal ATA is the margin wallet — principal and collateral must never share a vault; (3) runs are strictly sequential: `settle_run` returns the FULL searcher vault, so overlapping open runs corrupt the per-run `initial_pool_balance` snapshot and mint phantom realized yield. The `flag` is a recompute, NOT a persistent latch: `require!` rolls back the whole tx, so a breaching pull leaves the flag false (atomic). Flag becomes true only via non-reverting paths (e.g. `set_hedge` undercoverage) and then blocks withdrawals (withdraw while flagged reverts `/margin/` BEFORE the collateral check).
- Cluster-1 margin suite economics (authoritative, matches the test file): MAINT=200 bps, cap 10000. M1 post 100M; M2 withdraw→1M, pull 100M reverts /margin/ (atomic, flag stays false); M3 post 50M→51M, pull seq3 100M OK (reserve 830M), withdraw 50M reverts, withdraw 49M→pin floor 2M (2M*10000 == 100M*200 boundary); M4 hedge 100M→withdraw 2M→0, hedge 0 → flag trips, withdraw 1M reverts /margin/, post 2M clears, settle seq3 → gross 0, reserve 930M; M5 post 5M → collateral 7M. CCP then runs seq4 (emergency/exposure/share reverts, breaker 300M pull needs 6e10 <= 7e10 ✓) and seq5 (final 100M+20M). FINAL OVERALL END STATE (was runSeq 5): runSeq=6, statuses 1,2,1,2,1,0 (seq5 settled), timeouts 2, margin collateral=escrow=7M, gross=hedged=0, flag=false, gateway handshake pinned.
- Base-10 test economics after Phase 1 (authoritative, matches both suites): jlf_capital=800M/50%=400M, max_exposure=5B, share cap=10000bps. Flow: seq0 win 50M gross → net 40M/DIF 10M; seq1 fully-drained lose → JLF 100M (below breaker); omni vault inits at highwater=40M net; seq2 win 100M gross → net 80M/DIF 20M; sweep 120M; settle_day gross=120-40=80M → carry=80*17%=13.6M(+mgmt dust), ins=80*3%=2.4M, TA=1B+80*80%-mgmt≈1_064_000_000−mgmt, highwater 120M; DIF collect 30M→treasury(reserve 900M); emergency/exposure/share cap rejections then restored; seq3 pull 300M+full drain → force_close books +300M JLF = 400M == 50% boundary → **halts**; top-up 400M → 1.2B cap → un-halts; seq4 pull 100M+20M profit → net 16M/DIF 4M; final sweep 16M. END STATE: realized=swept=136_000_000, dif_ledger=4_000_000, timeout=2, runSeq=5 (statuses 1,2,1,2,1), jlfLosses=400M, jlfCapital=1.2B, dif treasury=30_000_000, reserve=604_000_000, vault highwater=120M, TA=carry+ins residual≈16_000_002, vaultUsdc holds residual+sweep surplus.
- Supply pump (Phase 3, gate `/* gate */` + omni `/* omni */`): `GateConfig.lender_pool: Pubkey` APPENDED after `dif_ledger` (+32 SPACE → `8+32+1+2+8+8+1+8+8+16+8+1+32*MAX_ASSETS+1+32+16+16+32`; raw realized offset preserved). Gate gains `register_lender(ctx, pool_vault)` (authority-gated) and `repay_supply(ctx, amount)` — requires `to_vault.owner == config.lender_pool` (UnknownLender, msg "supply recall target is not the registered lender pool"), rejects `to_vault == reserve_vault`, transfers reserve→to_vault signed via the gate PDA seeds `[tvv-gate, cfg.bump]`; anyone can call it but ONLY toward the registered lender (documented constraint). `VaultState.gate_loan: u64` APPENDED (SPACE → `8+32*4+1*3+8*4+16+2*3+8+8+8+1`). Omni gains `deploy_supply(ctx, amount)` (0<amount, !paused, `vault.authority==authority`, gate_linked, `vault_usdc.owner==vault`, `reserve_vault.owner==gate`, `gate==vault.gate`, amount<=vault_usdc balance; vault_usdc→reserve transfer signed by pool PDA `[omni-vault, usdc_mint, vault.bump]`; `gate_loan += amount`) and `recall_supply(ctx, amount)` (amount<=gate_loan → InsufficientLedger, `gate_loan -= amount`, typed CPI `noviscia_tvv_gate::cpi::repay_supply` with `RepaySupply` accounts). New omni errors GateMismatch/WrongReserveOwner; omni `Cargo.toml` depends on `noviscia-tvv-gate = { path = "../noviscia-tvv-gate", features = ["cpi"] }`. CRITICAL CPI gotcha: the pool's `RecallSupply` context must declare `#[account(mut)]` on the gate `AccountInfo` — the inner callee needs config WRITABLE, and the outer account's mutability carries into the CPI header (`"writable privilege escalated"` otherwise). Both instructions authority-gated (signer-only was a security hole).
- Pump suite economics (authoritative, appended AFTER the CCP describe): LP re-deposits 300M (vault total_shares 300M, totalAssets ≈316M incl. existing residual); register_lender(vaultPda); deploy 200M → reserve 604→804M, gate_loan 200M; second deploy reverts /liquidity/ (physical ~150M < 200M); unauthorized deploy reverts /authority only/; seq6 pull 50M + 10M mint → settle: net +8M, DIF +2M (realized 144M, dif_ledger 6M); recall 300M over-claim reverts /ledger/; recall 200M via CPI → vault +200M, gate_loan 0, reserve →614M; unregistered `repay_supply` to a random ATA reverts /registered lender/; collect_dif 2M → treasury 32M (dif_ledger back to 4M); sweep 8M → realized=swept=144M. **TRUE FINAL OVERALL END STATE (post pump): runSeq=7** (settled seq0,2,3,5,6; timeout seq1,4), realized=swept=144_000_000, dif_ledger=4_000_000, dif treasury=32_000_000, reserve=604_000_000 (reconciled exactly), jlfLosses=400M/jlfCapital=1.2B unhalted, committed 0, margin collateral=escrow=7M gross=hedged=0 flag=false, `GateConfig.lender_pool`==vault PDA, `gate_loan`=0, vault total_shares=300M/totalAssets≈316.05M, highwater=120M.
- Layout contract: gate `realized_yield` at data offset `[8+68 .. 8+84]`; omni reads raw offset; GateConfig fields must append AFTER `sweeper`/`swept_yield` (Phase 3 appended `lender_pool` last — raw offsets preserved).
- Bump contract: init contexts use bare `bump` (auto-canonical); each init fn ALSO writes the canonical bump into account data via `Pubkey::find_program_address` because `bump` fields are NOT auto-populated — stored bumps feed later CPI seeds (`cfg.bump`, `run.bump`, `searcher_state.bump`, `vault.bump`). Verified: tvv-gate=254, omni-vault=250, searcher/nshares=255.
- Omni vault init is TWO instructions: `initialize` (vault PDA only, sets fixups) then `initialize_shares` (mint, constraint `vault.authority==authority`). `VaultState::SPACE` = `8 + 32*4 + 1*3 + 8*4 + 16 + 2*3 + 8 + 8 + 1` (four Pubkeys incl `gate`).
- Redeem economics: `redeem` credits out-of-shareholders provisions; `usdc_out = shares * (total_assets - carry_ledger - insurance_ledger) / total_shares`. Full-exit leaves `total_assets = carry+ins` as a claimable residual — ledgers are the protocol's claim; DO NOT zero them on exit (they equal physical tokens still in the vault).
- anchor 0.31/0.32 client gotchas: `new Program(idl, provider)` (never pass idl.address as 2nd arg); after construction `idl.accounts` names are **camelCase** (`gateConfig`/`run`/`searcherState`/`vaultState`) although the raw JSON is PascalCase — `coder.accounts.decode` requires the camelCase name ("Account not found: GateConfig"); bn.js has no `toBigInt` (use `toArray('le',8)`); `PublicKey.findProgramAddress` on web3 1.99 is async-promised.
- Spec gotcha: the sentinel test suite must NOT import `chai` (not a dependency). Use Node's `assert` (`import assert from 'assert'`) exactly like `matrix.spec.ts`; a missing-dep require error gets masked by mocha's ESM fallback as `SyntaxError: Cannot use import statement` at line 1.
- Validator ops (repeat-after-me): (1) kill the old validator with `pgrep -f '^solana-test-validator' | xargs -r kill` — NEVER `pkill -f solana-test-validator` or `pgrep -f` unanchored (self-match kills the shell); (2) verify `ss` shows 8899 free; (3) `rm -rf test-ledger`; (4) start fresh via `( setsid solana-test-validator --reset --bpf-program <id> <so> ... > /tmp/nov-validator.log 2>&1 &)` in a DEDICATED bash call; (5) health-check in the NEXT call; (6) run tests once per validator — a second run hits `account already in use`.
- Running the mocha suite multiple times on one validator silently corrupts the intended assertions (already-in-use, duplicated state); treat the validator as a single-shot test bench per reset.
- On-chain state after a full clean suite run: see the Phase 1 end-state bullet above (realized=swept=136M, dif 4M in ledger + 30M in treasury, reserve 604M, highwater 120M, risk unhalted, committed 0).
- Known economics wrinkle: yield realized BEFORE the vault links (the "highwater gap") can still be swept into the vault but is never NAV-stamped → un-claimable surplus above TA (harmless; worth a prod decision on whether the sentinel sweeps only post-link realized).

## Work State
### Completed
- **Supply pump (Phase 3, this session):** gate `lender_pool`/`register_lender`/`repay_supply` (`ManageLender`/`RepaySupply` ctxs, `UnknownLender` error, authority check); omni `gate_loan`/`deploy_supply`/`recall_supply` (`DeploySupply`/`RecallSupply` ctxs, `GateMismatch`/`WrongReserveOwner`, typed CPI via `features=["cpi"]` path dep, `#[account(mut)]` on the CPI gate account — the writable-privilege gotcha). Both new pool instructions authority-gated. Gate unit **9/9** + omni unit **5/5**; rebuild in the `noviscia-tvv` workspace (a stray `anchor build` had run in the IGNORED sibling repo).
- **Integration suite 28/28 on a fresh geyser validator:** the 20 prior cases + an 8-test pump describe (LP re-fund + register lender; authority-gated deploy 200M; over-lending /liquidity/ revert; seq6 borrow banks 8M net + 2M dif; over-claim /ledger/ revert; CPI recall repay 200M; unregistered repay_supply /registered lender/ revert; collect_dif 2M + sweep 8M reconcile reserve to 604M). Validator must be restarted with the NEW .so (pump instructions do not exist in the old binary); the fixture was the `#account(mut)` CPI fix + a 400M (not 150M) over-lend attempt to clear physical liquidity.
- Off-chain smoke **13/13 passing** (`test:offchain:all`): `GateMirror.lenderPool` + `VaultMirror.gateLoan` decoded, `printMatrix` renders both, spec updated to the pump end-state (realized=swept=144M, dif treasury 32M, total_shares 300M, reserve 604M, difLedger 4M) + a new case "mirrors the supply pump: lender registered, loan settled to zero". CLI `sentinel matrix` verified rendering `lenderPool`==vault and `gateLoan:0`.
- **Cluster-1 margin & netting engine on the gate (Phase 2, prior session):** `MARGIN_SEED = b"tvv-margin"`; `MarginState` PDA per searcher (SPACE 100) with `collateral/gross_exposure/hedged_exposure/maintenance_bps/flag`; `init_searcher` gains `(cap_bps, maintenance_bps, enabled)` and inits the margin PDA; new instructions `post_margin`, `withdraw_margin`, `set_hedge` (+ contexts PostMargin/WithdrawMargin/SetHedge); `pull_cap` gates on `projected_margin_breached` and increments `gross_exposure`; `settle_run`/`force_close` decrement `gross_exposure` (by `run.principal`) and recompute the flag; all three also enforce the gateway handshake. `RiskState.gateway` appended (SPACE 141→173). New errors `WrongGateway`, `MarginBreach`, `InsufficientCollateral`, `HedgeExceedsExposure`. `anchor build` green; gate unit tests **9/9** (4 new margin-math cases pin the exact maintenance boundary; `>` vs `>=` semantics).
- **Integration suite 20/20 on a fresh geyser validator** (`tests/noviscia-flow.ts`): the 15 prior cases + a 5-test Cluster-1 describe (registration/handshake, atomically-rolled-back breach, top-up clears the flag + exact floor pin, hedge-releases-margin + settle back to principal, re-post through the CCP window). Registration now builds a DISTINCT raw run vault (explicit keypair) + searcher-ATA margin wallet + escrow; CCP renumbered to seq4/seq5. Root-cause work: `createAccount` ATA-collapse (phantom +98M), flag-vs-atomicity assertion bug, and the strict-sequencing rule all fixed and documented.
- Off-chain smoke **12/12 passing** on the live geyser validator (`test:offchain:all`): new `MarginMirror` + `searcherMargin` map + `marginEscrow`/`marginEscrowBalances` in `PositionMatrix` (`seeds().margin`), `RiskMirror.gateway`, `searcher.ts` `openRun`/`closeRun` now pass `risk`+`margin` (with `gatePda`/`riskPda`/`marginPda` helpers), sentinel `margin_call` ActionKind (emitted for flagged margins with uncovered exposure, suppressed while halted/emergency), `printMatrix` margins + risk.gateway output, and a new spec case "mirrors the margin engine and gateway handshake" (collateral=escrow=7M, gross=hedged=0, flag=false, maint=200, gateway==program id) + a no-margin_call assert on the settled policy. CLI `sentinel matrix` verified rendering the margin section.
- **Phase 1 CCP core on the gate (prior session):** `RiskState` PDA (seeds `[tvv-risk, gate]`): emergency_halt, JLF capital/losses, 50% circuit breaker (inclusive boundary), aggregate max_exposure + per-asset concentration caps, committed-exposure ledger decremented on settle/force_close. `GateConfig.dif_ledger` (appended, offset contract preserved). `set_risk_config` (halt/exposure/share/top-up, recloses breaker) + `collect_dif` (20% structural DIF → `[tvv-dif]` treasury ATA). `settle_run` 80/20 split; `force_close` books full principal into JLF and trips the breaker. New gate unit tests (dif split, breaker boundary). Off-chain mirrors updated: `RiskMirror`+`difLedger`+risk/dif treasury in `PositionMatrix`, `collect_dif` action + `autoCollectDif` flag + execution in SentinelEngine, risk/dif in `printMatrix`.
- On-chain suite + **20/20 localnet integration tests green** (`tests/noviscia-flow.ts`): original 11 + CCP describe (DIF collect to treasury, emergency halt, exposure/concentration caps, JLF breaker trips at exactly 50%, top-up unlock, lending resumes + final new-econ numbers) + 5-test Cluster-1 margin describe (handshake, atomic breach, floor pin, hedge release, re-post).
- Off-chain smoke **12/12 passing** on the live geyser validator (`test:offchain:all`): matrix.spec (live, gate+risk parse at the full end-state, 5-run enumeration, reflexive searcher discovery, **margin engine + gateway handshake**, fully-redeemed vault + 30M dif treasury, zero-actions settled policy incl. no margin_call, TTL, "proposes DIF settle when autoCollectDif") + ingest.spec (3 geyser cases).
- Prior-session on-chain work remains intact under the new numbers (stored-bump fix, `VaultState::SPACE` fix, redeem provisions fix). Off-chain control plane under `services/sentinel/src/` (strict TS, builds to `dist/`, root npm scripts):
  - `matrix.ts` — `PositionMatrix` mirror: parses gate/vault/searcher/run accounts via IDL, reflexively discovers searchers from runs, computes run PDAs from `run_seq` + candidate searchers, ATA token balances (reserve, searcher vaults, vault). `connect()` auto-derives usdc mint from `gate.assets[0]` and the vault PDA; exposes `gatePda`/`vaultPda`/`usdcMint`.
  - `sentinel.ts` — `SentinelEngine.tick()` returns actions: `sweep_profits`, `settle_day`, `collect_carry`/`collect_insurance`, `collect_dif` (behind `autoCollectDif`, blocked while halted/emergency), `ttl_alert`. `execute()` dispatches on-chain via provider wallet (DIF path getOrCreates the treasury ATA).
  - `ingest.ts` — `IngestSource` with `PollingSource` (default) and `YellowstoneSource` (typed against the real `@triton-one/yellowstone-grpc` v7 API): `Client` + `client.subscribe()` + `stream.on('data')`, subscribes to watched PDAs + operator wallet + slots (`filterByCommitment:false`), auto-reconnects on `error`/`close`, tallies `slotUpdates`/`accountUpdates`.
  - `searcher.ts` — `SearcherClient` (SEARCHER_KEYPAIR) with `openRun`/`closeRun`.
  - `index.ts` CLI: `matrix` | `watch` | `settle`; chooses YellowstoneSource over PollingSource when `GEYSER_ENDPOINT` set (logs `watching via geyser|poll`); watch keys = gate/vault/searcher PDAs + operator pubkey.
- Off-chain smoke (pre-Phase-1 baseline) was 10/10; now 11/11 with the DIF case. Geyser localnet rig: `libyellowstone_grpc.so` (release22 variant) at `~/.local/lib/`, config `~/.local/etc/geyser-config.json`, validator started with `--geyser-plugin-config`, gRPC on `127.0.0.1:10000`. Live `watch` verified: sentinel runs over the stream with zero spurious actions on settled state.
- Known root-cause chain established pre-session: stored-bump writes; `VaultState::SPACE` under-alloc (32*3-&gt;32*4); redeem provisions deduction; grpc package install + typed rewrite; slot/account filter semantics for localnet.
- System rules respected: no commits, no docs/comments.

### Active
- None — supply pump (Phase 3) implemented and verified end-to-end: gate **9/9** + omni **5/5** unit, integration **28/28**, off-chain **13/13** on the live geyser validator. Validator currently holds the clean post-pump end state (runSeq 7, realized=swept=144M, dif treasury 32M, reserve 604M, margin escrow 7M, lenderPool pinned, gate_loan 0) for the off-chain layer to operate on.

### Blocked
- Yellowstone ingest is LIVE on localnet via the polling-compatible `YellowstoneSource`. True production readiness needs a real geyser endpoint (devnet/mainnet provider gRPC, e.g. Triton `https://<provider>/yellowstone-grpc` style with `x-token`) — the same source object accepts it via `GEYSER_ENDPOINT`/`GEYSER_XTOKEN`.

## Next Move
- (Phase 2 candidates from the 4-Cluster audit — vault→gate pump now DONE; remaining) a `RiskState`-aware governance + JLF funding instruction (on-chain treasury), a timed/signer-gated `sweep_profits` for prod, and deciding whether `force_close` should try to claw back the stranded searcher-vault balance instead of writing off full principal.
- (New from Phase 3) the supply pump is currently authority-fixed to ONE lender pool with no tenure/fee; consider a lender auction (bids bps for the `deploy_supply` window), per-lender `gate_loan` proportional recall, and an on-chain `withdraw_reserve` guard so the admin cannot shrink the reserve below outstanding `gate_loan` (today `withdraw_reserve` is not loan-aware).
- (Margin engine hardening) consider an on-chain `force_margin_refresh`/liquidation path driven by the sentinel's `margin_call`, a hedge-account oracle feed instead of searcher-attested `set_hedge`, and sweeping the cross-cluster handshake (`gateway`) forward once the omni cluster gets its own gateway field.
- (Prod path) Point the sentinel at a real geyser endpoint: `GEYSER_ENDPOINT=https://<provider>/yellowstone-grpc GEYSER_XTOKEN=<token>`, add a `watchdog`/idempotence layer (heartbeat, never settle/sweep within the same epoch twice, per-searcher quotas, `set_sweeper` signer rotation), then deploy the pair to devnet at the real ids from `.keys/` (avoid the past accidental random-id devnet deploy).
- Optionally: split `collect_carry`/`collect_insurance` disbursement policy per design doc (insurance cold wallet vs carry treasury); wire `CompressedAccountFilterSet` for large watch sets.

## Relevant Files
- `programs/noviscia-tvv-gate/src/lib.rs` — gate + CCP core (RiskState + gateway, dif_ledger, set_risk_config, collect_dif, JLF breaker) + Cluster-1 margin engine (MarginState, post_margin/withdraw_margin/set_hedge, margin gates in pull/settle/force_close) + Phase 3 supply pump (lender_pool, register_lender, repay_supply, ManageLender/RepaySupply, UnknownLender).
- `programs/noviscia-omni-pool/src/lib.rs` + `Cargo.toml` — vault logic (split init_shares, redeem provisions, stored vault bump; raw-offset read of gate realized) + Phase 3 supply pump (gate_loan, deploy_supply/recall_supply, DeploySupply/RecallSupply, GateMismatch/WrongReserveOwner; typed CPI against the gate crate via `features=["cpi"]`; `#[account(mut)]` on the CPI gate account).
- `tests/noviscia-flow.ts` — 28-case integration suite: original 11 + CCP/DIF describe + 5-test Cluster-1 margin describe + 8-test pump describe (uses persisted `.searcher-key.json`; distinct raw run vault via explicit keypair, searcher-ATA margin wallet, margin escrow).
- `services/sentinel/src/{config,matrix,sentinel,ingest,searcher,index}.ts` — off-chain control plane (RiskMirror.gateway + GateMirror.lenderPool + VaultMirror.gateLoan; MarginMirror/searcherMargin maps + margin escrow balances; margin_call sentinel action; searcher.openRun/closeRun wired risk+margin; typed YellowstoneSource in `ingest.ts`).
- `services/sentinel/test/{matrix,ingest}.spec.ts` — off-chain smoke, 13 cases, env-gated on live RPC + geyser.
- `package.json` scripts `sentinel:*`, `test:offchain`, `test:geyser`, `test:offchain:all`; `Anchor.toml` (cluster=localnet); `.searcher-key.json` (searcher identity).
- `~/.local/lib/libyellowstone_grpc.so` + `~/.local/etc/geyser-config.json` — localnet geyser rig (gRPC on 127.0.0.1:10000).
- `/tmp/nov-validator.log`, `/tmp/nov-mocha.log`.